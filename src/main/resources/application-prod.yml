server:
  port: 8083

spring:
  datasource:
    url: jdbc:oracle:thin:@192.168.0.77:1522:ssodb
    username: SAO_REG_KEYCLOAK
    password: SAO_REG_KEYCLOAK
    driverClassName: oracle.jdbc.OracleDriver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.Oracle12cDialect

keycloak:
  server:
    contextPath: /auth
    adminUser:
      username: superadmin
      password: office@betimes.Biz
    realmImportFile: baeldung-realm.json